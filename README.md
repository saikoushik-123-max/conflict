# STEPS
created a floder called conflict
created a subfloder called index.html
entered the script 
moved index.html file from local repo to staging 
now time to commit the index.html
commited
now lets push to remote repo call git hub
git remote add origin "url form git hub"
git remote -v to check changes

